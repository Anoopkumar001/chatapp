# Leave empty / unset for local dev — it will fallback to localhost:5000
# Set these in Netlify/Vercel environment variables when deploying

VITE_API_URL=https://your-backend-url.onrender.com/api
VITE_SOCKET_URL=https://your-backend-url.onrender.com
